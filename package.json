{
  "name": "aiktivist",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "start": "bun run src/server.ts",
    "migrate": "bun run src/migrate.ts",
    "test": "bun test",
    "check": "bun test"
  }
}
